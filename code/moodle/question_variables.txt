v:x;
rdm:-1-rand(9);
rdm2: rand_with_prohib(-9,9,[0]);
pp:v^-rdm+rdm*exp(v);
p:rdm2*exp(pp);

/* Debug-Schalter des KI-Tutors: 1 = der Diagnose-Marker wird im
   PRT-Feedback uebertragen und der Code im Tutorbereich gezeigt (nur
   Entwicklung); 0 oder geloeschte Variable = kein Diagnosecode, der
   Tutor gibt nur allgemeine Hinweise. */
debug:0;

/* Auskommentiertes Experiment aus der bisherigen Frage. Das URL-Enkodieren
   uebernimmt jetzt der STACK-JS-Baustein mit URLSearchParams. Diese Funktion
   nur dann wieder einkommentieren, wenn eine alte Frage noch urlencode_basic
   oder den Validator validate_listlength referenziert; sie gehoert dann in
   dieselben Fragevariablen. In Moodle zusaetzlich die Input-Extra-Optionen
   auf Verweise pruefen (siehe README, Abschnitt Migration).

urlencode_basic(s) := block(
  [r],
  r : string(s),

  Prozentzeichen zuerst kodieren,

  r : ssubst("%25", "%", r),

  r : ssubst("%20", " ", r),
  r : ssubst("%2B", "+", r),
  r : ssubst("%26", "&", r),
  r : ssubst("%3D", "=", r),
  r : ssubst("%3F", "?", r),
  r : ssubst("%23", "#", r),
  r : ssubst("%2F", "/", r),
  r : ssubst("%5E", "^", r),
  r : ssubst("%28", "(", r),
  r : ssubst("%29", ")", r),
  r : ssubst("%2A", "*", r),
  r : ssubst("%2C", ",", r),

  url:r
)$
*/
